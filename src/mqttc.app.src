%% vim: set ft=erlang : -*- erlang -*- 
{application, mqttc,
 [
   {description, "An Erlang MQTT Client"},
  {vsn, git},
  {registered, [mqttc_sup, mqttc_session_sup]},
  {applications, [
                  kernel,
                  stdlib,
                  inets,
                  mqttm,
                  splay_tree
                 ]},
  {mod, { mqttc_app, []}},
  {env, []}
 ]}.
